<UserControl x:Class="KHSave.SaveEditor.Kh3.Views.PlayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KHSave.SaveEditor.Kh3.Views"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="400">
    <Grid>
        <TabControl>
            <TabItem Header="Status">
                <ScrollViewer HorizontalScrollBarVisibility="Auto">
                    <local:PlayerStatusView/>
                </ScrollViewer>
            </TabItem>
            <TabItem Header="Equipment">
                <ScrollViewer HorizontalScrollBarVisibility="Auto">
                    <local:PlayerEquipmentView/>
                </ScrollViewer>
            </TabItem>
            <TabItem Header="Ability">
                <local:PlayerAbilityView/>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
