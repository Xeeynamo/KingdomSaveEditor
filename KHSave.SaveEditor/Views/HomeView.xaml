<UserControl x:Class="KHSave.SaveEditor.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KHSave.SaveEditor.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="500">
    <UserControl.Resources>
        <SolidColorBrush x:Key="textForeground" Color="#F1F1F1" />
        <SolidColorBrush x:Key="textHyperlink" Color="#569CD6" />
        <Thickness x:Key="labelMargin" Left="10" Top="0" Right="0" Bottom="3" />
    </UserControl.Resources>
    <StackPanel>
        <Label>No save is currently load.</Label>
        <Label>Load the save from File\Open...</Label>
        <Separator Margin="0 10 0 10"/>
        <Label>Kingdom Hearts Save Editor currently supports the following games:</Label>
        <StackPanel Margin="20 0 0 0">
            <TextBlock>Kingdom Hearts II: Final Mix (PS2,PS4)</TextBlock>
            <TextBlock>Kingdom Hearts Re: Chain of Memories (PS2,PS4)</TextBlock>
            <TextBlock>Kingdom Hearts 0.2</TextBlock>
            <TextBlock>Kingdom Hearts III</TextBlock>
        </StackPanel>
        <Separator Margin="0 10 0 10"/>
        <TextBlock Margin="{StaticResource labelMargin}" Foreground="{StaticResource textHyperlink}">
            <Hyperlink
                NavigateUri="https://discord.gg/qK8JYkw"
                RequestNavigate="Hyperlink_RequestNavigate"
                TextDecorations="{x:Null}">
                Join Save Editor Discord server
            </Hyperlink>
        </TextBlock>
        <TextBlock Margin="{StaticResource labelMargin}" Foreground="{StaticResource textHyperlink}">
            <Hyperlink
                NavigateUri="https://github.com/xeeynamo/kh3saveeditor"
                RequestNavigate="Hyperlink_RequestNavigate"
                TextDecorations="{x:Null}">
                Kingdom Hearts Save Editor source code
            </Hyperlink>
        </TextBlock>
        <TextBlock Margin="{StaticResource labelMargin}" Foreground="{StaticResource textHyperlink}">
            <Hyperlink
                NavigateUri="https://github.com/xeeynamo/openkh"
                RequestNavigate="Hyperlink_RequestNavigate"
                TextDecorations="{x:Null}">
                OpenKh source code
            </Hyperlink>
        </TextBlock>
        <TextBlock Margin="{StaticResource labelMargin}" Foreground="{StaticResource textHyperlink}">
            <Hyperlink
                NavigateUri="https://xeeynamo.github.io/OpenKh"
                RequestNavigate="Hyperlink_RequestNavigate"
                TextDecorations="{x:Null}">
                OpenKh documentation
            </Hyperlink>
        </TextBlock>
        <TextBlock Margin="{StaticResource labelMargin}" Foreground="{StaticResource textHyperlink}">
            <Hyperlink
                NavigateUri="https://lucianociccariello.com/"
                RequestNavigate="Hyperlink_RequestNavigate"
                TextDecorations="{x:Null}">
                Author website
            </Hyperlink>
        </TextBlock>
        <Separator Margin="0 10 0 10"/>
        <Grid Margin="{StaticResource labelMargin}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Image Grid.Column="0" Margin="0 0 5 0" Width="32"
                        Source="/KHSave.SaveEditor;component/Images/Patreon.png"/>
            <StackPanel Grid.Column="1">
                <TextBlock Grid.Column="1" TextWrapping="Wrap"
                        Text="Fund me on Patreon to be featured below and help me to improve the Save Editor!"/>

                <TextBlock Foreground="{StaticResource textHyperlink}">
                    <Hyperlink
                        NavigateUri="https://www.patreon.com/khsaveeditor"
                        RequestNavigate="Hyperlink_RequestNavigate"
                        TextDecorations="{x:Null}">
                        https://www.patreon.com/khsaveeditor
                    </Hyperlink>
                </TextBlock>
            </StackPanel>
        </Grid>
        <TextBlock Text="Patrons:"/>
        <WrapPanel Name="patronList"/>
    </StackPanel>
</UserControl>
