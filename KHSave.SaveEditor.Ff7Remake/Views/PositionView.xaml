<UserControl x:Class="KHSave.SaveEditor.Ff7Remake.Views.PositionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KHSave.SaveEditor.Ff7Remake.Views"
             mc:Ignorable="d" 
             d:DesignHeight="22" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0" Text="X" Margin="0 0 3 0"/>
        <TextBlock Grid.Column="3" Text="Y" Margin="0 0 3 0"/>
        <TextBlock Grid.Column="6" Text="Z" Margin="0 0 3 0"/>
        <TextBox Grid.Column="1" Text="{Binding X}"/>
        <TextBox Grid.Column="4" Text="{Binding Y}"/>
        <TextBox Grid.Column="7" Text="{Binding Z}"/>
    </Grid>
</UserControl>
